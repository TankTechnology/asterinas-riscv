<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!-- Minimal fontconfig config for the RISC-V desktop smoke test.
     Self-contained: does not pull in /etc/fonts/conf.d. Maps the generic
     families onto the one bundled font (Adwaita Sans) so pango/GTK2 can
     resolve a usable sans-serif. -->
<fontconfig>
  <dir>/usr/share/fonts</dir>
  <cachedir>/tmp</cachedir>

  <match target="pattern">
    <test qual="any" name="family"><string>sans-serif</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Adwaita Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>serif</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Adwaita Sans</string></edit>
  </match>
  <match target="pattern">
    <test qual="any" name="family"><string>monospace</string></test>
    <edit name="family" mode="prepend" binding="strong"><string>Adwaita Sans</string></edit>
  </match>
</fontconfig>
